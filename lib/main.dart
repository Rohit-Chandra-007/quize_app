import 'package:flutter/material.dart';

void main() => runApp(QuizeApp());

class QuizeApp extends StatelessWidget {
  const QuizeApp({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Text("Hello World"),
    );
  }
}
